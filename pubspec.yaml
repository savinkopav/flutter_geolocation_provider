name: flutter_geolocation_provider
description: Simple android geolocation provider
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  pigeon: ^10.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.savinkopav.flutter_geolocation_provider
        pluginClass: FlutterGeolocationProviderPlugin